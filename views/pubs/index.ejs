    <% layout('layouts/boilerplate') %>
    <div id="map" style="width: 100%; height: 500px"></div>
    <h1>All Pubs</h1>
    <div>
        <a href="/pubs/new">Add Pub</a>
    </div>
    <ul>
        <% for (let pub of pubs) { %>
            <div class="card mb-3">
                <div class="row">
                    <div class="col-md-4">
                        <% if (pub.images[0]) { %>
                             <img class="img-fluid" alt="" src="<%= pub.images[0].url %>">
                             <% } else { %>
                                <img class="img-fluid" alt="" src="https://res.cloudinary.com/pubhub/image/upload/v1641661269/PubHub/noimage_uipwgk.jpg"></img>
                            <% } %> 
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= pub.title %></h5>
                            <p class="card-text"><%= pub.description %></p>
                            <p class="card-text">
                                <small class="text-muted"><%= pub.location %></small>
                            </p>
                            <a href="/pubs/<%= pub._id %>" class="btn btn-primary">View Pub</a>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>
    </ul>
    <script>
        const mapToken = '<%- process.env.MAPBOX_TOKEN %>';
        const pubs = {features: <%- JSON.stringify(pubs) %>}
    </script>
    <script src="/scripts/clusterMap.js"></script>